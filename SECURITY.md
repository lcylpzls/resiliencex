# 安全说明

resiliencex 将以下情况视为安全缺陷:

- 组件并发访问导致数据竞争或状态错乱;
- 限流/熔断被绕过(如负参数、时钟回拨导致限流失效);
- 拒绝服务:极端参数导致内存或 CPU 失控。

## 报告漏洞

- 请勿在公开 issue 中披露漏洞细节;
- 通过邮件 [lcylpzls@qq.com](mailto:lcylpzls@qq.com) 联系维护者,
  并在标题注明 `[Security]`;
- 修复发布前我们会与报告者保持沟通;发布后可应要求公开致谢。

## 安全使用建议

- 组件配置在启动期确定,不要在运行期动态替换参数;
- 熔断窗口与限流速率按业务峰值 + 余量设置;
- 舱壁许可必须在 defer 中释放,防止泄漏;
- 错误消息不包含敏感业务数据。
